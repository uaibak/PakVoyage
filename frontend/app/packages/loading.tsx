import { PageLoader } from '@/components/page-loader';

export default function Loading() {
  return (
    <PageLoader
      eyebrow="Packages"
      title="Loading travel packages"
      description="Preparing fixed departures, dates, and seat availability for your next Pakistan trip."
    />
  );
}
