import { PageLoader } from '@/components/page-loader';

export default function Loading() {
  return (
    <PageLoader
      title="Welcome to PakVoyage"
      description="Loading latest destinations and featured travel packages for you."
    />
  );
}