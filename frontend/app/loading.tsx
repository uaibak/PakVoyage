import { PageLoader } from '@/components/page-loader';

export default function Loading() {
  return (
    <PageLoader
      eyebrow="Preparing trip planner"
      title="Loading PakVoyage"
      description="Pulling together destinations, route ideas, and travel details for Pakistan."
    />
  );
}
