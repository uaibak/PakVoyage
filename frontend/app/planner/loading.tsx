import { PageLoader } from '@/components/page-loader';

export default function Loading() {
  return (
    <PageLoader
      eyebrow="Planner"
      title="Opening the trip planner"
      description="Setting up your planning form so you can choose days, budget, and interests."
    />
  );
}
